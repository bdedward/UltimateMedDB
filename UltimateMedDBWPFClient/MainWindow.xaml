<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UltimateMedDBWPFClient"
        xmlns:WPFClient="clr-namespace:UltimateMedDB.WPFClient" x:Class="UltimateMedDBWPFClient.MainWindow"
        mc:Ignorable="d"
        Title="Ultimate Medical DB" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <WPFClient:UltimateMedDBViewModel/>
    </Window.DataContext>

    <Grid>
        <StackPanel >
            <TextBox Text="Patient Lookup" />
            <ComboBox Name="cboAllPatients"  SelectedItem ="{Binding SelectedPatient}" ItemsSource="{Binding AllPatients}" DisplayMemberPath="Name" Width="250" />
            <DataGrid ItemsSource="{Binding SelectedPatient.Patients}" CanUserAddRows="True" />

            <TextBox Text="Add a New Patient" />
            <WrapPanel>
                <Label>Name</Label>
                <TextBox Width="140" Text="{Binding NewPatient.Name}" />
                <Label>Gender</Label>
                <TextBox Width="80" Text="{Binding NewPatient.Gender}" />
                <Label>Age</Label>
                <TextBox Width="30" Text="{Binding NewPatient.Age}" />
                <Label>Phone</Label>
                <TextBox Width="140" Text="{Binding NewPatient.Phone}" />
                <Label>Weight</Label>
                <TextBox Width="30" Text="{Binding NewPatient.Weight}" />
                <Label>Address</Label>                
                <TextBox Width="190" Text="{Binding NewPatient.Address}" />
                <Label>Doc_Id</Label>
                <TextBox Width="48" Text="{Binding NewPatient.Doc_Id}" />
                <Label>Disease</Label>
                <TextBox Width="85" Text="{Binding NewPatient.Disease}" />
            </WrapPanel>
            <Button Click="Button_Click" Width="125">Save New Patient</Button>

            <ComboBox ItemsSource="{Binding AllBills}" DisplayMemberPath="InsuranceCarrier" Width="150"/>

            <ComboBox ItemsSource="{Binding PatientLabs}" DisplayMemberPath="Amount" Width="100"/>

        </StackPanel>
    </Grid>
</Window>
